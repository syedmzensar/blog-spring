#db configuration

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blogapp
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
 